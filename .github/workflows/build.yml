name: Build
on:
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - name: Setup node.js
        uses: actions/setup-node@v6
        with:
          node-version: 14.x
      - run: npm i
      - run: npm run build
